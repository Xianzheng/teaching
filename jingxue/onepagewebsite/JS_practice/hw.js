// json data structure

//     key:value

obj = {msg:['1.jpg','2.jpg','3.jpg']}

// we can treat obj as json data structure

console.log(obj)

// if we want to access the value
// ['1.jpg','2.jpg','3.jpg'] is an array or list
console.log(obj.msg)

obj.msg.forEach(element => {
    console.log(element)
});

// after we public picture, we can see it in moment
// if you get any question , send me the code and question image
